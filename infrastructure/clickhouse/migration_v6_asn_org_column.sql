-- =============================================================================
-- 迁移 v6：新增 asn_org 列
-- -----------------------------------------------------------------------------
-- 用途：存储 ASN 组织名称（如 "The Constant Company" / "China Telecom"），
--       用于前端访问日志页面的「IP 详情」列展示机房/运营商名称。
--
-- MMDB reader 已经解析出该字段，但之前没落库。
-- =============================================================================

ALTER TABLE fangyu.decision_events 
ADD COLUMN IF NOT EXISTS asn_org String DEFAULT '' 
AFTER asn;
